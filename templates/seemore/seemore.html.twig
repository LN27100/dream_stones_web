<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link href="{{ asset('assets/styles/app.css') }}" rel="stylesheet">

    <title>Dream Stones - détails</title>
</head>

<body>
    <header id="top">
        {% include 'includes/header.php' %}
    </header>

        <div id="vue-app"></div> {# Contenu du panier Vue.js #}


        <h1>Détails de la pierre</h1>

<div class="container">
    <div class="row">
        <div class="col-lg-8">
            <div id="mainImage" class="card">
                <div class="card-image">
                    <img src="{{ asset('assets/images/' ~ product.productPicture) }}" alt="Image principale du produit">
                </div>
                <div class="card-body">
                    <h5 class="card-title">{{ product.productName }}</h5>
                    <p class="card-text">{{ product.productDescription }}</p>
                    <p class="card-text">Référence: {{ product.productReference }}</p>
                    <p class="card-text">Pays d'origine: {{ product.productOriginCountry }}</p>
                    <p class="card-text">Couleur: {{ product.productColor }}</p>
                    <p class="card-text">Stock: {{ product.productStock }}</p>
                    <p class="card-text">Prix: {{ product.productPrice }} €</p>
                    <div class="container text-center mt-3">
                        <button class="btn2 btn-add-to-cart" data-id="{{ product.id }}" data-name="{{ product.productName }}" data-price="{{ product.productPrice }}" data-picture="{{ product.productPicture }}"><i class="bi bi-cart-fill"></i> Ajouter au panier</button>
                    </div>
                </div>
            </div>
        </div>
       <div class="col-lg-4">
    <div class="row">
       {% for image in images %}
    <img src="{{ asset('assets/images/' ~ image.imagePath) }}" alt="Images associée au produit">
    {% endfor %}
    </div>
</div>
        </div>
    </div>
</div>


    <footer>
        {% include 'includes/footer.php' %}
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
<script type="module" src="/assets/js/app.js"></script>


<script>
    // JavaScript pour gérer le changement d'image au clic sur une miniature
    const mainImg = document.getElementById('mainImg');
    const thumbnails = document.querySelectorAll('.thumbnail img');

    thumbnails.forEach(thumbnail => {
        thumbnail.addEventListener('click', () => {
            mainImg.src = thumbnail.src;
        });
    });
</script>
</body>

</html>
